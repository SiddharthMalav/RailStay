/**
 * Provides common enums.
 */

export enum eHTTPStatusCode {
  OK = 200,
  CREATED = 201,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  INTERNAL_SERVER_ERROR = 500,
  VALID_REQUEST = 299,
}

export enum sizes {
  sm = "text-sm",
  md = "text-md",
  lg = "text-lg",
  xl = "text-xl",
}
